<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Eumar Rice Mill - Product List</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding-top: 60px;
      color: #34495e;
      position: relative;
      overflow-x: hidden;
      background-color: #f5f7f4;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background-image: url('rice-bg.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      filter: blur(3px) brightness(0.85);
      z-index: -1;
    }

    header {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 60px;
      background-color: #2ecc71;
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      font-weight: 700;
      box-shadow: 0 2px 6px rgba(0,0,0,0.2);
      z-index: 999;
      overflow: hidden;
    }

    header .moving-text {
      position: absolute;
      white-space: nowrap;
      animation: slideRight 20s linear infinite;
    }

    @keyframes slideRight {
      0% { left: -100%; }
      100% { left: 100%; }
    }

    .side-nav {
      position: fixed;
      top: 60px;
      left: 0;
      width: 200px;
      height: calc(100vh - 60px);
      background-color: #2ecc71;
      padding-top: 20px;
      box-shadow: 2px 0 6px rgba(0,0,0,0.15);
      display: flex;
      flex-direction: column;
      align-items: center;
      z-index: 998;
    }

    .side-nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
      width: 100%;
    }

    .side-nav ul li {
      margin: 12px 0;
      text-align: center;
    }

    .side-nav ul li a {
      color: white;
      text-decoration: none;
      font-weight: 700;
      font-size: 1.1rem;
      display: block;
      padding: 12px 0;
      border-radius: 8px;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .side-nav ul li a:hover {
      background-color: #27ae60;
      box-shadow: 0 0 10px #27ae60;
    }

    main.dashboard-container {
      margin-left: 220px;
      padding: 30px;
    }

    main h1 {
      text-align: center;
      color: #2c3e50;
      margin-bottom: 25px;
      font-size: 1.8rem;
    }

    .product-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      padding: 25px;
      animation: fadeIn 0.4s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
      font-size: 1rem;
    }

    th, td {
      padding: 14px 10px;
      text-align: center;
    }

    th {
      background-color: #27ae60;
      color: white;
      font-weight: 600;
    }

    tr:nth-child(even) {
      background-color: #f8f9f7;
    }

    tr:hover {
      background-color: #eafaf1;
      transition: background-color 0.2s ease;
    }

    td {
      color: #2c3e50;
    }

    .no-products {
      text-align: center;
      font-style: italic;
      color: #888;
      margin-top: 20px;
      font-size: 1.1rem;
    }

    @media (max-width: 992px) {
      .side-nav {
        position: relative;
        width: 100%;
        height: auto;
        box-shadow: none;
        flex-direction: row;
        justify-content: center;
      }

      .side-nav ul {
        display: flex;
        gap: 20px;
      }

      main.dashboard-container {
        margin-left: 0;
        padding: 20px;
      }

      table {
        font-size: 0.9rem;
      }

      th, td {
        padding: 10px;
      }
    }
    
.qr-code-container {
  margin-top: 20px;
  padding: 15px 10px;
  border-radius: 10px;
  color: white;
  text-align: center;
  font-size: 0.9rem;
  line-height: 1.3;
  user-select: none;
}

.qr-code-container img {
  width: 120px;
  height: auto;
  border-radius: 8px;
  margin-bottom: 8px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3);
}

.qr-code-caption {
  font-weight: 700;
  margin-bottom: 6px;
  color: #f1fdf5;
  user-select: text;
}

@media (max-width: 992px) {
  .qr-code-container {
    margin-top: 10px;
    padding: 10px 5px;
  }

  .qr-code-container img {
    width: 90px;
  }
}

  </style>
</head>
<body>
  <header>
    <span class="moving-text">Welcome to Eumar Rice Mill</span>
  </header>

  <nav class="side-nav" aria-label="Main Navigation">
    <ul>
       <li><a href="/customer">Home</a></li>
      <li><a href="/join-queue">Join Queues</a></li>
       <li><a href="/transactions">Transaction History</a></li>
       <li><a href="/customer/products">Today’s Picks</a></li>
    <li><a href="/system-guide">System Guide</a></li>
  </ul>
   <div class="qr-code-container" aria-label="Payment QR Code">
<img src="/eumar-gcash.jpg" alt="QR Code for Payment" />
    <p class="qr-code-caption">Scan to Pay</p>
  <p>Open your GCash app, select "Scan QR", and point your camera to this code.</p>
</div>
  </nav>
  
  <main class="dashboard-container">
    <div class="product-card">
      <h1>Available Rice Products</h1>

      <% if (products.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>Item Name</th>
              <th>Category</th>
              <th>Quantity (sacks)</th>
              <th>Unit Price (₱)</th>
              <th>Last Updated</th>
            </tr>
          </thead>
          <tbody>
            <% products.forEach(product => { %>
              <tr>
                <td><%= product.item_name %></td>
                <td><%= product.category %></td>
                <td><%= product.quantity %></td>
                <td>₱<%= Number(product.unit_price).toFixed(2) %></td>
                <td><%= new Date(product.updated_at).toLocaleString() %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <p class="no-products">No available rice products at the moment.</p>
      <% } %>
    </div>
  </main>
</body>
</html>
